"use strict";

var $ = {
  cookie: function cookie(key, value) {
    var json = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};
    var cookie_str = encodeURIComponent(key) + '=' + encodeURIComponent(value); //有效期

    if (!isNaN(json.expires)) {
      var date = new Date();
      date.setDate(date.getDate() + json.expires);
      cookie_str += ';expires=' + date;
    } //路径


    if (json.path) {
      cookie_str += ';path=' + json.path;
    }

    document.cookie = cookie_str;
  },
  getCookie: function getCookie(key) {
    var arr = document.cookie.split('; ');

    for (var i = 0, len = arr.length; i < len; i++) {
      var list = arr[i].split('=');

      if (encodeURIComponent(key) === list[0]) {
        return decodeURIComponent(list[1]);
      }
    }
  },
  removeCookie: function removeCookie(key) {
    var json = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};

    if (json.path) {
      document.cookie = "".concat(encodeURIComponent(key), "=;path=").concat(json.path, ";expires=").concat(new Date(0));
    } else {
      document.cookie = "".concat(encodeURIComponent(key), "=;expires=").concat(new Date(0));
    }
  }
};