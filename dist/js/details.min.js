"use strict";

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); return Constructor; }

//滚动
$(window).scroll(function () {
  var top = $(document).scrollTop();
  console.log(top);

  if (top >= 200) {
    $('#run').css('opacity', '1');
    $('#run').css('position', 'fixed');
    $('#run').css('background', 'white'); // $("#run").slideDown();
  } else {
    $('#run').css('opacity', '0');
  }
}); //弹窗

$(function () {
  $('#p3').click(function () {
    $('#popout').show();
  });
  $('#p3 a').click(function () {
    $('#popout').show();
  });
  $('#wrong').click(function () {
    $('#popout').hide();
  });
}); // product

var Product = /*#__PURE__*/function () {
  function Product() {
    _classCallCheck(this, Product);

    this.$chose = $('#chose'); //获取颜色

    this.$pen = $('#pen');
    this.$pencil = $('#pencil');
    this.$suit = $('#suit'); //减号

    this.$minus = $('minus'); //加号

    this.$plus = $('#plus'); //购物车

    this.$cart = $('#cart'); //加入购物车

    this.$join = $('#join'); //立即购买

    this.$buy = $('#buy'); //添加事件

    this.addEvent();
  }

  _createClass(Product, [{
    key: "addEvent",
    value: function addEvent() {
      //记录实例对象
      var that = this; // alert(that);

      this.$pen.click(function () {
        that.$pen.css('border', '#df1010');
        that.$pen.css('color', '#df1010');
        var $penval = that.$pen.text();
        that.$chose.text($penval);
      });
      this.$pencil.click(function () {
        that.$pencil.css('border', '#df1010');
        that.$pencil.css('color', '#df1010');
        var $pencilval = that.$pencil.text();
        that.$chose.text($pencilval);
      });
      this.$suit.click(function () {
        that.$suit.css('border', '#df1010');
        that.$suit.css('color', '#df1010');
        var $suitval = that.$suit.text();
        that.$chose.text($suitval);
      }); //查看购物车按钮

      this.$cart.click(function () {
        location.href = '../html/cart.html';
      }); //遍历加入购物车

      this.$join.each(function () {});
    }
  }]);

  return Product;
}();

new Product();